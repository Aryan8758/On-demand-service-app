<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme -->
    <style name="Base.Theme.ForYou" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <!-- Custom Dialog Theme -->
    <style name="MyDialogTheme" parent="Theme.Material3.DayNight.Dialog.Alert">
        <item name="android:buttonBarPositiveButtonStyle">@style/MyDialogButton</item>
        <item name="android:buttonBarNegativeButtonStyle">@style/MyDialogButton</item>
        <item name="android:buttonBarNeutralButtonStyle">@style/MyDialogButton</item>
    </style>

    <!-- Custom Dialog Button Style -->
    <style name="MyDialogButton" parent="Widget.Material3.Button.TextButton">
        <item name="android:textColor">#03DAC5</item> <!-- Change button text color -->
    </style>

</resources>
